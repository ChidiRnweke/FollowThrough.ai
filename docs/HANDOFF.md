# Docs work — handoff plan (remaining work)

Context: big docs overhaul of the Starlight site in `docs/`. Done so far: theme aligned to
DESIGN_SYSTEM.md, home page rebuilt (app hero + user/dev tile split), self-hosting guide, favicon

- TypeDoc `gitRevision: 'master'` + `categorizeByGroup: false` fixes, top-nav dropdowns
  (`docs/src/components/nav-extras.astro`), sidebar split into User/Developer documentation,
  pagination + sidebar font taming, and 9 subagent-written subsystem pages. **Nothing is committed
  yet** — all work is uncommitted in the working tree.

## 1. Fix broken cross-links between the new pages (highest risk)

Subagents cross-linked pages whose slugs differ from what was actually written. Grep and fix:

- `docs/src/content/docs/subsystems/agent.mdx`, `memory.mdx` link to
  `/FollowThrough.ai/subsystems/knowledge-search/` → the search page is
  `subsystems/search.mdx` → `/FollowThrough.ai/subsystems/search/`.
- `subsystems/notes.mdx` links to `/subsystems/diagrams/` and `/subsystems/suggestions/`;
  `subsystems/settings.mdx` links to `/subsystems/suggestions/`;
  `subsystems/skills.mdx` links to `/subsystems/provenance/` — **none of those pages exist**.
  Either retarget to the nearest existing page (`agent.mdx` covers suggestions/approval flow;
  `notes.mdx` covers diagrams) or drop the links. Cheapest correct move: retarget, don't create
  new pages.
- Verify with: `grep -rno '/FollowThrough\.ai/[a-z/-]*/' docs/src/content/docs | sort -u` and
  compare against the actual routes after build (`ls docs/dist`).

## 2. Missing screenshot assets

`skill-support` and `tools` pairs exist in `static/product-screenshots/` but were never copied.
`subsystems/settings.mdx` uses `<Screenshot id="tools">`; `subsystems/skills.mdx` deliberately
omits `skill-support` (see agent note in its page comments/summary). Fix:

```sh
cd static/product-screenshots
cp skill-support-light.jpg skill-support-dark.jpg tools-light.jpg tools-dark.jpg \
   ../../docs/public/screenshots/
```

Then optionally add the `skill-support` Screenshot to `subsystems/skills.mdx`.

## 3. Sidebar registration (`docs/astro.config.mjs`)

The new pages are not in the sidebar yet. Update the two groups:

- **User documentation**: Getting started → then `using/` pages (The agent
  `/using/the-agent/`, Todos, Notes, Memory, Attachments, Search, Projects) → Agents propose,
  users accept (`/explanation/agents-propose-users-accept/`).
- **Developer documentation**: Self-hosting → How-to guides (autogenerate `guides`, collapsed)
  → **Subsystems** (`{ label: 'Subsystems', collapsed: true, items: [{ autogenerate: { directory: 'subsystems' } }] }`)
  → `typeDocSidebarGroup` → Architecture.
- Decide whether `using/` autogenerates or is explicit links; explicit links give control over
  order and labels. Note `using/` is currently NOT autogenerated anywhere — pages are orphaned
  until this is done.

## 4. Build + repair loop

```sh
pnpm -F @followthrough/docs build
```

Expect possible failures: MDX import-depth mistakes (`../../../components/…` is correct from
`using/` and `subsystems/`), Mermaid `source` prop quoting, frontmatter errors. Fix as they
surface. Then serve `docs/dist` (`npx astro preview --port 4510` from `docs/`) and check:

- home page, both themes (no sidebars, hero crayon, spacing);
- one `using/` page and one `subsystems/` page — mermaid renders (no "Syntax error in text"),
  screenshots swap with theme;
- a `reference/` page — sidebar now module → pages (no Functions/Interfaces layer), group
  labels quiet (sm/500), "Defined in" links contain `/blob/master/`;
- pagination footer — title at 0.875rem, not billboard size;
- top-nav "User docs"/"Developer docs" dropdowns open, close on outside click/Escape.

Playwright is installed at the root: `npx playwright screenshot --wait-for-timeout=6000
--viewport-size=1280,900 <url> /tmp/x.png`.

## 5. Unification + QA pass over the 15 new pages

- Skeleton consistency: user pages = TL;DR → intent → what it does → what you can do → where to go next; dev pages =
  TL;DR → intent → models → services → controllers → repositories + invariants + diagrams where they earn their place.
- Voice grep (should return nothing): `grep -rniE 'this (page|section|guide) (covers|describes|documents)|methodology' docs/src/content/docs/using docs/src/content/docs/subsystems`.
- Spot-check the factual claims each agent reported (they are listed in the swarm results —
  e.g. chunk sizes 2400/480, 50 MB attachment limit, `LOCKED_TOOL_NAMES`, run state machine)
  against the cited files. Trust but verify ~5 claims per page.
- The attachments agent flagged one inference (removed image no longer loads in notes) — verify
  or soften.
- Copy tone reference: `docs/src/content/docs/guides/write-api-docstrings.mdx`.

## 6. Atomic commits (standing user instruction: commit atomically from now on)

Suggested split (stage hunks, not whole tree at once):

1. `docs: align theme tokens and typography with the app design system` — `docs/src/styles/theme.css`
2. `docs: rebuild home page with app hero and user/developer split` — `index.mdx`, `home-hero.astro`, `crayon.astro`, `home-section.astro`, `resource-row.astro`
3. `docs: add self-hosting guide` — `self-hosting/`, sidebar entry hunk
4. `docs: fix favicon base path and reference source links` — favicon/`gitRevision` hunks of `astro.config.mjs`
5. `docs: fix root-relative content links` — the 4 link fixes in getting-started/guides
6. `docs: add screenshots and mermaid diagrams to content pages` — `screenshot.astro`, `mermaid.astro`, page embeds, `docs/public/screenshots/`, mermaid dep (`docs/package.json`, `pnpm-lock.yaml`)
7. `docs: add top-nav doc group dropdowns and GitHub link` — `nav-extras.astro`, `social`/`components` hunk
8. `docs: split sidebar into user and developer documentation` — sidebar hunk
9. `docs: tame pagination and sidebar typography` + `docs: flatten API reference sidebar` (`categorizeByGroup`) — can be 1–2 commits
10. `docs: document subsystems for users and developers` — `using/`, `subsystems/`, sidebar registration (split per subsystem if diffs stay clean)

Then push — `.github/workflows/docs-pages.yml` already deploys `docs/dist` to GitHub Pages on
push to master (verified complete: checkout → pnpm → build → upload-pages-artifact →
deploy-pages). Nothing to change there.

## 7. Cleanup

- Kill the leftover preview server: `kill 34009` (port 4510) — verify with `ss -ltnp | grep 4510`.
- `docs/HANDOFF.md` (this file) — delete after the work lands, don't commit it.
