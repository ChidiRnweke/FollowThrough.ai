---
title: Todos
description: How commitments in notes become reviewable, provenance-carrying todos, through extraction, scoring, kanban status changes, waiting-on tracking, and the Today view.
---

import Mermaid from '../../../../components/mermaid.astro';

Todos track work. Users can create tasks directly or extract commitments from selected note text.
Extraction prepares a durable request, generates candidates outside the publication transaction and
then publishes anchors, provenance, suggestions and any auto-accepted tasks together. Each extracted
task retains its source sentence, note revision and producer. The shared suggestions controller owns
review, acceptance, rejection and reversal.

User-facing behavior: [Todos](/FollowThrough.ai/using/todos/).

## Why this subsystem exists

Meeting notes and project write-ups are full of promises, like "Sarah will send the draft by Friday." Left in prose they rot. The todos subsystem extracts those promises into structured records with an owner, a due date, and a strength/confidence score, but it refuses to do so silently: extraction always lands as a suggestion first (unless the user has explicitly trusted the pipeline), and every todo remembers its origin sentence so review is a comparison, not a guess.

## Where the pieces live

- **Models**: `src/lib/models/todos/index.ts`: `Todo`, `TodoView`, `PromiseCandidate`, and the status/responsibility/strength unions.
- **Services**: `src/lib/server/services/todos/`: `TodoCatalog` (`catalog.ts`) resolves and persists task records; shared `services/todos/` owns edit and presentation rules; `promise-extraction/` is the LLM extractor; `promise-rules.ts` is the deterministic fallback extractor.
- **Controllers**: `src/lib/server/controllers/todos/controller.ts`: the `Todos` class, the documented public surface (`get`, `list`, `count`, `listCategories`, `create`, `update`, `remove`, `extractPromises`). The Today view lives in `src/lib/server/controllers/workspace/controller.ts` (`getTodayView`).
- **Repositories**: `src/lib/server/repositories/todos/` (`postgres/todos.ts`): all Drizzle queries; rows are mapped to models at the boundary.

## Durable extraction and publication

The task controller freezes selection and generation context in the durable request. Queued recovery
uses that recorded input. The provider runs outside the publication transaction. Settlement claims
the run before anchor, provenance, proposal, effect and completion writes commit together. A failed
publication leaves no orphan anchor or half-created suggestion. Relative dates use the original run
time, so recovery does not reinterpret “tomorrow” using the recovery date.

<Mermaid
	source={`
flowchart LR
	sel["text selection"] --> anchor["SourceAnchor: quote + note revision"]
	sel --> ext["PromiseExtractor"]
	ext --> cand["candidates: action, owner, strength, confidence 0-100"]
	anchor --> prov["Provenance: pipeline = extract_promises"]
	cand --> sug["one todo suggestion per candidate"]
	prov --> sug
	sug --> trust{"trust policy auto-accept?"}
	trust -- "yes" --> todo["create todo + mark suggestion auto-accepted"]
	trust -- "no" --> pending["suggestion stays proposed"]
`}
/>

Two details worth knowing:

- A task suggestion carries a `CreateTodoInput`. At acceptance, the Suggestions controller applies the shared creation rule and asks TodoCatalog to persist the resolved task. Project, anchor and provenance checks run against current records.
- The suggestion gets `noteId`, the anchor, and the provenance id; the payload additionally bakes in `sourceAnchorId` and `provenanceId`, so the todo created on accept keeps the full chain.

### Scoring and extraction

The controller depends on a `PromiseExtractor` contract with two implementations:

- `PromiseDiscovery` (LLM, `promise-extraction/`): an OpenRouter chat model with zod-enforced structured output covering action, owner, responsibility, verbatim and resolved due date, strength, and a 0 to 100 confidence. Falls back when no API key is configured.
- `DeterministicPromiseExtractor` (`promise-rules.ts`): a sentence splitter plus a `promise(s) to / will / should / might` grammar. It skips questions and any sentence containing `could`/`optionally`; maps the marker to `explicit` (95), `implied` (70), or `tentative` (55) confidence; assigns `mine` when the owner is "I"/"we" and `waiting_on` otherwise; and resolves only the word "tomorrow" to a date, while other verbatim dates carry through unresolved.

Auto-accept is decided per suggestion by `ToolTrust.shouldAutoAccept` (`src/lib/server/services/agent/runs/tool-trust.ts`): the user's trust policy for `extract_promises` must have auto-accept enabled, and the suggestion's confidence must clear the policy's `minimumConfidence` when one is set. The default policy is `autoAcceptEnabled: false`, so extraction proposes unless the user opted in.

## Task creation

Task and suggestion controllers apply `services/todos/creation.ts` before persistence. Direct
creation, durable batches, automatic acceptance and reviewed acceptance use that same rule. It
normalizes the title, keeps caller-assigned identities and resolves the initial status, counterparty
and completion time. Offline commands apply the same rule with their recorded identity and time.
TodoCatalog checks ownership and referenced records before inserting the resolved task. Models hold
values, request types and Zod schemas only.

## Task edits and status semantics

`TodoStatus` is `backlog | open | in_progress | done | cancelled`. A board drag is a task edit.
The Todos controller starts a transaction, locks and reads the current active task, applies the
shared edit rule and persists one complete result. Concurrent edits to different fields therefore
retain both changes. The task catalog validates linked records before saving.

The shared rule stamps `completedAt` when status changes to `done`, preserves it when editing an
already completed task and clears it when reopening. It clears `waitingOn` when responsibility
becomes `mine`. Null clears a nullable field; omitted fields keep their current values. Downloaded
workspace commands use the same rule for optimistic edits.

A linked note must be an active ordinary note in the same project. An edit cannot move the task to
another project. A request with no edit fails. Deleted tasks are unavailable to identity reads, and
a stale write cannot clear their deletion marker. The controller assembles the returned view through
the same presentation service as downloaded views: a linked note takes display precedence while the
extraction origin remains separately visible.

## Waiting-on tracking

`TodoResponsibility` is `mine | waiting_on`. A waiting-on todo names its counterparty in `waitingOn` (free text, e.g. "Sarah"). `TodoCatalog.findWaitingOn` is just `list({ responsibility: 'waiting_on' })`. There is no separate state machine; waiting-on is a filter, and clearing it is an edit, not a transition.

## Today view

`Workspace.getTodayView` (`src/lib/server/controllers/workspace/controller.ts:93`) aggregates four reads in parallel:

- `todoLister.list({ dueBefore: today, responsibility: 'mine' })`: everything due up to and including today, split into `overdue` (due date before today, undated items excluded) and `dueToday` (due date exactly today).
- `waitingOnFinder.findWaitingOn`: all waiting-on todos.
- `suggestionLister.countByStatus('proposed')`: the pending-review count.
- `noteTreeReader.list`: for `pinnedNotes` and the five most recently updated `recentNotes`.

Due-date filtering in the repository is `dueDate <= dueBefore` (`postgres/todos.ts`), ordered due-date ascending then recently updated. `count` runs without assembling views because the agent context bar calls it on every navigation.

## Guarantees callers lean on

- `extractPromises` is all-or-nothing: anchor, provenance, suggestions, and auto-accepted todos commit together or roll back together.
- A `todo` suggestion's payload is a complete `CreateTodoInput`; accepting it re-runs all create validation, so a suggestion can never create a todo that `create` would reject.
- An extracted todo always traces to its source: `sourceAnchorId` → quote + note revision, `provenanceId` → pipeline + producer. `TodoView.originNote` always means the extraction source; `sourceNote` is the linked note if set, otherwise the origin.
- `completedAt` is set if and only if status is `done`.
- Deletion is soft (`deletedAt`); history survives.
- A suggestion transition is a compare-and-swap on expected status, so a double accept or a decision on an already-decided suggestion throws `InvalidTransitionError` rather than silently winning. Revert requires an `appliedArtifactId` and soft-deletes the todo the accept created.

## Neighboring pieces

- [Notes](/FollowThrough.ai/subsystems/notes/): selections, `SourceAnchor`, and `Provenance` are notes-capability types; extraction starts from a note revision.
- [Agent](/FollowThrough.ai/subsystems/agent/): `extractPromises` is registered as a proposal-kind agent tool (`src/lib/server/factories/agent/agent-tool-factory.ts`), and the trust policies that gate auto-accept are agent-run policy. Suggestions review UI also lives beside the agent.
- Search: todos themselves are not indexed; the note text they came from is.
