// The node itself lives in `nodes.ts` so the server shares its Markdown handlers;
// this module keeps the import path used by `note-editor.svelte`.
export { TodoNode } from './BuiltinExtensions.js';
